import React, { PropTypes } from 'react'

const Hero = ({props}) => {
  return (
    <div>
      Page Hero
    </div>
  )
}

// Hero.defaultProps = {
//   // myProp: 300
// };

// Hero.propTypes = {
//   // PropType Requirement
// };

export default Hero