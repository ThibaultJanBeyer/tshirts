import React, { PropTypes } from 'react'

const Header = ({props}) => {
  return (
    <header>
      Page Header
    </header>
  )
}

// Header.defaultProps = {
//   // myProp: 300
// }

// Header.propTypes = {
//   // PropType Requirement
// }

export default Header